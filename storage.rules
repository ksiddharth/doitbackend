rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /ephemeral/{sessionId}/{allPaths=**} {
      allow write: if request.auth != null;
      allow read: if false;
    }
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
